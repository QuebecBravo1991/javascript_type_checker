# JavaScript Type Checker
This project is a simple type checker for JavaScript. It is built for a small subset of JavaScript where the only type of value is `number`. This subset also contains if and if else statemetns, while blocks, functions, simple expressions, and local variables. 

I built this to learn a little about static analysis and type checking.
